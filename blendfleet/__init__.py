# Duplicated from pyproject.toml's [project] version on purpose: the frozen
# build has no package metadata to read, and a crash log that cannot name
# the build it came from is a crash log nobody can act on.
__version__ = "0.1.0"
